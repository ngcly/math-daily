.page {
  min-height: 100vh;
  background: var(--paper);
}
.content {
  padding: 24rpx 24rpx 48rpx;
}
.page-title {
  display: block;
  font-size: 48rpx;
  font-weight: 900;
  color: var(--ink);
  letter-spacing: -1rpx;
  margin-bottom: 24rpx;
}
.section-card {
  background: var(--white);
  border-radius: 24rpx;
  padding: 24rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.06);
}
.section-card__title {
  display: block;
  font-size: 26rpx;
  font-weight: 800;
  color: var(--ink);
  margin-bottom: 24rpx;
}
.section-card__row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16rpx 0;
  border-bottom: 1rpx solid var(--ink-5);
}
.section-card__row:last-child {
  border-bottom: none;
}
.section-card__row--btn {
  border-bottom: none;
  padding-top: 16rpx;
}
.section-card__label {
  font-size: 28rpx;
  color: var(--ink-2);
  flex-shrink: 0;
}
.section-card__value {
  font-size: 26rpx;
  color: var(--ink-3);
  font-weight: 600;
  max-width: 55%;
  text-align: right;
}
.section-card--about .section-card__title {
  color: var(--ink-3);
}
.profile-avatar-row {
  display: flex;
  justify-content: center;
  padding: 16rpx 0 24rpx;
}
.avatar-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
  background: transparent;
  border: none;
  padding: 0;
  line-height: 1;
}
.avatar-btn::after {
  display: none;
}
.avatar-btn:active {
  opacity: 0.75;
}
.avatar-img {
  width: 128rpx;
  height: 128rpx;
  border-radius: 50%;
  border: 3rpx solid var(--ink-5);
}
.avatar-placeholder {
  width: 128rpx;
  height: 128rpx;
  border-radius: 50%;
  background: var(--paper);
  border: 3rpx dashed var(--ink-5);
  display: flex;
  align-items: center;
  justify-content: center;
}
.avatar-placeholder__icon {
  font-size: 52rpx;
  line-height: 1;
}
.avatar-hint {
  font-size: 20rpx;
  color: var(--ink-4);
  font-weight: 500;
}
.nickname-input {
  flex: 1;
  text-align: right;
  font-size: 28rpx;
  color: var(--ink);
  font-weight: 600;
  min-height: 44rpx;
}
.nickname-input__placeholder {
  color: var(--ink-4);
  font-weight: 400;
}
.about-footer {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  padding: 24rpx 0 16rpx;
}
.about-footer__quote {
  font-size: 22rpx;
  font-weight: 400;
  color: var(--ink-3);
  line-height: 1.75;
  text-align: center;
  letter-spacing: 0.5rpx;
}
.about-footer__copy {
  font-size: 22rpx;
  color: var(--ink-4);
}
.picker-value {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 28rpx;
  color: var(--ink-2);
  font-weight: 700;
}
.picker-arrow {
  font-size: 32rpx;
  color: var(--ink-4);
}
.outline-btn {
  width: 100%;
  height: 80rpx;
  border: 2rpx solid var(--ink-5);
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 26rpx;
  color: var(--ink-2);
  font-weight: 600;
  transition: all 150ms;
}
.outline-btn:active {
  background: var(--paper);
}
.outline-btn--disabled {
  opacity: 0.4;
  pointer-events: none;
}